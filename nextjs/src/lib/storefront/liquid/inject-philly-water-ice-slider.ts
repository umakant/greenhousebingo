/**
 * Placeholder: some dev setups still have this path in Tailwind/Turbopack’s file graph
 * after an earlier experiment; missing it caused ENOENT during `globals.css` processing.
 * Hero slider images live in the theme package (`slide_image_*.png`), not here.
 */
export {};
